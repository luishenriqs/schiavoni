export type UserDTO = {
  name: string;
  email: string;
  profile: string;
  avatar: string;
  doc_id: string;
  isAdmin: boolean;
};

export type SquadOfPlayersDTO = {
  label: string;
  value: string;
};
