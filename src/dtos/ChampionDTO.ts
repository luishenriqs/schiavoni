export type ChampionDTO = {
  name: string;
  email: string;
  profile: string;
  avatar: string;
  doc_id: string;
  season: number;
};

export type HallOfChampionsDTO = {
  player: string;
  season: number;
  doc_id: string;
};
